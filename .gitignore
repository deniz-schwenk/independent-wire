# Environment
.env
.env.local
.env.production

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.venv/
venv/
env/

# Pipeline output (regenerated per run)
output/
state/
raw/

# Agent memory (persistent, not committed)
memory/

# IDE & OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
# Logs
*.log
logs/

# Testing
.coverage
htmlcov/
.pytest_cache/
.gstack/

# Claude Code internals
CLAUDE.md
.claude/

# Internal planning & session files (anywhere in repo)
**/SESSION-HANDOFF-*.md
**/TASK-*.md
**/WP-*.md
**/BRIEFING-*.md
docs/handoffs/
docs/archive/
docs/COST-OPTIMIZATION-ANALYSIS.md

# Architect workspace — local only, never committed
briefs/
BRIEF-*.md
PROMPT-ENGINEER-*.md
_pre_v2_archive/
V2-*-SMOKE-LOG.md

# Sweep harness embedding cache (binary, regeneratable)
docs/gravitational-recalibration-*/_cache/

# Cluster-internal-audit harness cache (pre-cluster output, binary/regeneratable)
docs/cluster-internal-audit/audit-*/_cache/

# Cluster-internal-audit working helpers (local-only; superseded by committed CSVs)
scripts/_audit_*

# Cluster-level gravitation sweep similarity-matrix cache (binary, regeneratable)
docs/cluster-level-gravitation-*/_cache/

# Architect-internal workbench notes (BACKLOGs, post-mortems, diagnostic reports)
docs/internal/

# Bulk calibration / audit-trail data dirs (workbench, not public surface)
docs/cluster-internal-audit/
docs/cluster-quality-audit/
docs/cluster-llm-assignment-*/
docs/cluster-level-gravitation-*/
docs/coherence-filter/
docs/cost-efficiency-sweep-*/
docs/curator-monitor/
docs/curator-shadow/
docs/curator-topic-discovery/
docs/curator-variance-*/
docs/gravitational-assign/
docs/gravitational-recalibration-*/
docs/pre-cluster/
docs/triple-stage-cutover/

# Transient architect/CC workspace — never stage (multi-GB model artifacts
# live here). NOTE: 19 legacy-tracked files under scratch/bias-eval/ remain
# tracked (ignore only affects untracked); dedup vs docs/evals pending.
scratch/

# Cowork operational docs are local-only (never on GitHub)
COWORK-*.md
